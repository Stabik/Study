@model IEnumerable<ClassLibrary.model.Document>

@{
    ViewBag.Title = "LoadAllDocumnetById";
}

<h2>LoadAllDocumnetById</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameFolder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FolderType)
        </th>
        <th>
         
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DocType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameFolder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FolderType)
        </td>
        <td>
       
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DocType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
