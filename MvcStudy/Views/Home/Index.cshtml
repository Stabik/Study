@using MvcStudy.Models
@model HomeModel
@{
    ViewBag.Title = Model.Title;
}

@using (Html.BeginForm())
{
    <div>
        @Html.DisplayNameFor(m => m.Title)
    </div>
    <div>
        @Html.EditorFor(m => m.Title)
        @Html.HiddenFor(m => m.Time)
    </div>
    <div>
        <input type="submit" value="Отправить" />
    </div>
}
<h2>@Model.Time</h2>

@Html.ActionLink("Создать пользователя", "Create", "Person")
@Html.ActionLink("Создать Группу", "Create", "Group")
@Html.ActionLink("Создать User", "CreateGroup", "Group")
@Html.ActionLink("Создать Doc", "Create", "Document")
@Html.ActionLink("Создать SuperFolder", "Create", "SuperFolder")



